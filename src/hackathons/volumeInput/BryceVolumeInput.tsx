import { Box, Typography, useTheme } from "@mui/material";

export function BryceVolumeInputV1() {
  const theme = useTheme();

  return (
    <Box sx={{ p: 4, borderRadius: 2, bgcolor: "background.paper", boxShadow: theme.shadows[4] }}>
      <Typography variant="h5" sx={{ mb: 2 }}>Bryce's Volume Input V1</Typography>
      {/* Implementation goes here */}
    </Box>
  );
}

export function BryceVolumeInputV2() {
  const theme = useTheme();

  return (
    <Box sx={{ p: 4, borderRadius: 2, bgcolor: "background.paper", boxShadow: theme.shadows[4] }}>
      <Typography variant="h5" sx={{ mb: 2 }}>Bryce's Volume Input V2</Typography>
      {/* Implementation goes here */}
    </Box>
  );
}

export function BryceVolumeInputV3() {
  const theme = useTheme();

  return (
    <Box sx={{ p: 4, borderRadius: 2, bgcolor: "background.paper", boxShadow: theme.shadows[4] }}>
      <Typography variant="h5" sx={{ mb: 2 }}>Bryce's Volume Input V3</Typography>
      {/* Implementation goes here */}
    </Box>
  );
} 
